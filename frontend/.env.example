# Optional. Leave unset in development — the Vite dev server proxies /api to the
# backend (see vite.config.ts), which keeps requests same-origin.
#
# Set this for a production build where the API lives on another host, e.g.
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=
